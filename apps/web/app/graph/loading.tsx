export default function GraphLoading() {
  return (
    <div
      style={{
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        minHeight: "100vh",
        background: "var(--bg-0)",
        color: "var(--ink-faint)",
        fontFamily: "var(--mono)",
        fontSize: 12,
      }}
    >
      Loading graph…
    </div>
  );
}
