export default function Loading() {
  return (
    <div style={{ display: "flex", alignItems: "center", justifyContent: "center", minHeight: "100vh", color: "var(--ink-faint)", fontFamily: "var(--mono)", fontSize: 12 }}>
      Loading...
    </div>
  );
}
